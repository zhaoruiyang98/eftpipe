tracer:  # [tracerA, tracerB, cross]
data:
  # also support list
  # tracerA:
    # reader: auto
    # reader_kwargs: {}
    # path: ''
    # ls: [0, 2]
    # kmin: 0.01
    # kmax: 0.23
  # tracerB:
  # ...
cov:
  # reader: auto
  # reader_kwargs: {}
  # path: ''
  # Nreal: 1000
  # rescale: 1
chained: false  # also support list[bool] or dict[str, bool]
with_binning: false  # also support list[bool] or dict[str, bool]
binning:  # also support list[dict]
marg: